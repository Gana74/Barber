# Инструкция по развёртыванию Telegram-бота на VDS (Ubuntu 24.04 LTS "Noble Numbat" [x64]

# 1 ядро, 0,5 ГБ RAM, 10 ГБ NVMe)

## 📋 Оглавление

1. [Подготовка VDS](#1-подготовка-vds)
2. [Установка необходимого ПО](#2-установка-необходимого-по)
3. [Создание swap-файла](#3-создание-swap-файла-важно-для-серверов-с-малым-объемом-ram)
4. [Настройка проекта](#4-настройка-проекта)
5. [Настройка переменных окружения](#5-настройка-переменных-окружения)
6. [Создание необходимых директорий](#6-создание-необходимых-директорий)
7. [Настройка PM2 для продакшена](#7-настройка-pm2-для-продакшена)
8. [Проверка работоспособности](#8-проверка-работоспособности)
9. [Запуск и настройка автозапуска](#9-запуск-и-настройка-автозапуска)
10. [Проверка статуса](#10-проверка-статуса)
11. [Полезные команды для управления](#11-полезные-команды-для-управления)
12. [Мониторинг и обслуживание](#12-мониторинг-и-обслуживание)
13. [Устранение неполадок](#13-устранение-неполадок)
14. [Безопасность](#14-безопасность)
15. [Настройка уведомлений](#15-настройка-уведомлений)
16. [Примечания](#16-примечания)

---

## 1. Подготовка VDS

### Подключение к серверу по SSH:

```bash
ssh root@IP_АДРЕС_СЕРВЕРА
```

### Обновление системы:

```bash
apt update && apt upgrade -y
```

---

## 2. Установка необходимого ПО

### Установка Node.js 18+:

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs
node --version # Проверка установки (должно быть 18.x или выше)
```

### Установка PM2 для управления процессом:

```bash
npm install -g pm2
```

### Установка Git:

```bash
apt install -y git
```

---

## 3. Создание swap-файла (ВАЖНО для серверов с малым объемом RAM)

```bash
# Создание swap-файла 1GB
fallocate -l 1G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# Добавление в автозагрузку
echo '/swapfile none swap sw 0 0' >> /etc/fstab

# Оптимизация использования swap
echo 'vm.swappiness=10' >> /etc/sysctl.conf
sysctl -p

# Проверка
free -h
```

---

## 4. Настройка проекта

### Клонирование репозитория:

```bash
cd /opt
git clone <URL-ВАШЕГО-GIT-РЕПОЗИТОРИЯ> master-bot
cd master-bot
```

### Установка зависимостей:

```bash
npm install
```

---

## 5. Настройка переменных окружения

### Создание файла .env:

```bash
nano .env
```

### Добавление переменных (замените значения на свои):

```env
BOT_TOKEN=ваш_токен_бота
GOOGLE_SHEETS_ID=ID_вашей_таблицы
GOOGLE_CLIENT_EMAIL=service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
DEFAULT_TIMEZONE=Asia/Yekaterinburg
MANAGER_CHAT_ID=123456789
NODE_ENV=production
```

### Защита файлов:

```bash
chmod 600 .env
chmod 600 credentials.json 2>/dev/null || true
```

---

## 6. Создание необходимых директорий

```bash
mkdir -p logs
```

---

## 7. Настройка PM2 для продакшена

### Создание конфигурационного файла PM2:

```bash
nano ecosystem.config.js
```

### Конфигурация для серверов с ограниченной памятью (0.5-1GB RAM):

```javascript
module.exports = {
  apps: [
    {
      name: "master-bot",
      script: "index.js",
      env: {
        NODE_ENV: "production",
        NODE_OPTIONS: "--max-old-space-size=180", // Ограничение памяти Node.js
      },
      log_date_format: "YYYY-MM-DD HH:mm:ss",
      error_file: "logs/pm2-error.log",
      out_file: "logs/pm2-out.log",
      merge_logs: true,
      log_rotate: {
        max_size: "5M",
        retain: 2,
        compress: false,
      },
      max_memory_restart: "190M", // Перезапускать если память >190MB
      min_uptime: "30s",
      max_restarts: 5,
      autorestart: true,
      watch: false,
      time: true,
    },
  ],
};


```

### Конфигурация для серверов без ограниченной памяти:
<!-- module.exports = {
  apps: [{
    name: "barber-bot",
    script: "index.js",
    env: {
      NODE_ENV: "production"
    },
    log_date_format: "YYYY-MM-DD HH:mm:ss",
    error_file: "logs/error.log",
    out_file: "logs/out.log",
    time: true
  }]
} -->
---

## 8. Проверка работоспособности

### Тестовый запуск:

```bash
npm start
```

Если всё работает (нажмите Ctrl+C для остановки), переходите к следующему шагу.

---

## 9. Запуск и настройка автозапуска

### Запуск бота через PM2:

```bash
pm2 start ecosystem.config.js
```

### Сохранение конфигурации PM2 для автозапуска:

```bash
pm2 save
pm2 startup
```

### Настройка ротации логов PM2 (опционально):

```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 5M
pm2 set pm2-logrotate:retain 2
pm2 set pm2-logrotate:compress false
```

---

## 10. Проверка статуса

### Проверка состояния бота:

```bash
pm2 status
```

### Просмотр логов в реальном времени:

```bash
pm2 logs master-bot
```

### Проверка использования ресурсов:

```bash
pm2 monit
free -h
```

---

## 11. Полезные команды для управления

### Просмотр логов:

```bash
# Последние 50 строк логов
pm2 logs master-bot --lines 50

# Логи в реальном времени
pm2 logs master-bot

# Файловые логи
tail -f logs/pm2-error.log
tail -f logs/pm2-out.log
```

### Перезапуск бота:

```bash
pm2 restart master-bot
```

### Остановка бота:

```bash
pm2 stop master-bot
```

### Обновление кода:

```bash
cd /opt/master-bot
git pull
npm install
pm2 restart master-bot
```

### Просмотр потребления ресурсов:

```bash
pm2 monit
htop
free -h
```

---

## 12. Мониторинг и обслуживание

### Ежедневная проверка:

```bash
# Проверка статуса
pm2 status

# Проверка памяти
free -h

# Проверка логов на ошибки
grep -i "error\|failed" logs/pm2-error.log | tail -20
```

### Очистка старых логов:

```bash
# Очистка логов PM2
pm2 flush master-bot

# Ротация логов вручную
pm2 reloadLogs
```

---

## 13. Устранение неполадок

### Если бот не запускается:

```bash
# Проверка логов ошибок
pm2 logs master-bot --err

# Проверка файла .env
head -10 .env

# Проверка зависимостей
npm list --depth=0
```

### Если не хватает памяти:

```bash
# Проверка использования памяти
pm2 show master-bot

# Увеличение swap (если нужно)
fallocate -l 2G /swapfile2
chmod 600 /swapfile2
mkswap /swapfile2
swapon /swapfile2
```

### Если бот падает:

```bash
# Просмотр количества перезапусков
pm2 show master-bot

# Увеличение лимита памяти в конфиге
# Измените --max-old-space-size=200 в ecosystem.config.js
```

---

## 14. Безопасность

### Ограничение доступа к файлам:

```bash
chmod 700 /opt/master-bot
chmod 600 /opt/master-bot/.env
chmod 600 /opt/master-bot/credentials.json 2>/dev/null || true
```

### Регулярное обновление:

```bash
# Обновление системы
apt update && apt upgrade -y

# Обновление Node.js модулей
cd /opt/master-bot && npm update
```

---

## 15. Настройка уведомлений

Получать уведомления о падении бота в Telegram:

```bash
# Установка pm2-telegram
pm2 install pm2-telegram
pm2 set pm2-telegram:bot_token "YOUR_BOT_TOKEN"
pm2 set pm2-telegram:chat_id "YOUR_CHAT_ID"
```

---

## 16. Примечания

- Для серверов с 0.5GB RAM обязательно создайте swap-файл
- Регулярно проверяйте логи на наличие ошибок
- Не забывайте обновлять систему и зависимости
- Сохраняйте резервные копии конфигурационных файлов

---

## Визуализация процесса

```
Подготовка VDS
    ↓
Установка ПО (Node.js, Git, PM2)
    ↓
Создание swap-файла
    ↓
Настройка проекта (клонирование, зависимости)
    ↓
Настройка переменных окружения (.env)
    ↓
Создание конфига PM2
    ↓
Запуск через PM2
    ↓
Мониторинг и обслуживание
```

**Готово!**
